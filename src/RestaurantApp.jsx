import { AppTheme } from './theme/AppTheme';
import { AppRouter } from './router/AppRouter';

export const RestaurantApp = () => {
  return (
    <AppTheme>
      <AppRouter />
    </AppTheme>
  );
};
