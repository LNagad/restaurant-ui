import { Toolbar } from '@mui/material';
import { EditOrderModal, Example } from '../components';

export const Orders = () => {
  return (
    <>
      <Toolbar />
      <Example />
      <EditOrderModal />
    </>
  );
};
